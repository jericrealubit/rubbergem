/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Outputs a static build to the /out folder
  basePath: "/rubbergem",
  assetPrefix: "/rubbergem",
};

export default nextConfig;
